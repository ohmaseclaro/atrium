# @atriumjs/atrium-worker (deprecated)

This package re-exports [`@atriumjs/worker`](../../worker/README.md). Install `@atriumjs/worker` directly.

The `atrium-worker` binary forwards to `@atriumjs/worker`.
